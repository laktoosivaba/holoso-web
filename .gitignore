# build artifacts — regenerated by `make dist` (holoso wheel + vendored pyodide + assembled site)
wheels/*.whl
pyodide/
dist/

# python bytecode
__pycache__/
*.pyc

# node (tools/)
tools/node_modules/

# act config — absolute host paths (kubeconfig, buildkit certs), machine-specific
.actrc

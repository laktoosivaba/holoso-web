# build artifacts — regenerated by `make dist` (holoso wheel + vendored pyodide/yosys/nextpnr/demos + assembled site)
wheels/*.whl
wheels/extra-manifest.json
wheels/manifest.json
pyodide/
yosys/
nextpnr-ecp5/
demos/
dist/

# python bytecode
__pycache__/
*.pyc

# node (tools/)
tools/node_modules/

# act config — absolute host paths (kubeconfig, buildkit certs), machine-specific
.actrc

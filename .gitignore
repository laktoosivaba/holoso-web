# build artifacts — regenerated by `make dist` (holoso wheel + vendored pyodide/yosys/hdl + assembled site)
wheels/*.whl
pyodide/
yosys/
hdl/
dist/

# python bytecode
__pycache__/
*.pyc

# node (tools/)
tools/node_modules/

# act config — absolute host paths (kubeconfig, buildkit certs), machine-specific
.actrc
